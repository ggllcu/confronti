/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

.rich-text p {
    margin-bottom: 0.5rem;
}

.list-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    padding: 20px;

    div {
        flex: 1;
        margin: 20px;
        border-radius: 100px;
    }
}